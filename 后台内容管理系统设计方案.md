# 春晖官网后台内容管理系统设计方案

## 1. 目标

把春晖官网从“静态展示页”升级为“可维护的企业官网系统”：

- 前台是给客户看的多页面网站。
- 后台是给公司员工维护内容的管理系统。
- 员工不需要懂代码、不需要改文件名、不需要压缩图片。
- 登录后通过表单、上传图片、预览、发布完成内容更新。

## 2. 整体结构

```text
客户访问前台页面
        |
        v
前台网站读取数据库内容
        |
        v
后台管理系统修改数据库内容
        |
        v
管理员登录、上传图片、发布内容
```

建议最终结构：

```text
chunhui-web/
├── public/                 前台公开资源
│   ├── uploads/             后台上传后的图片
│   └── assets/              CSS、JS、图标
│
├── pages/                  前台页面模板
│   ├── index.html           首页
│   ├── products.html        产品中心
│   ├── product-detail.html  产品详情
│   ├── about.html           关于春晖
│   ├── news.html            新闻动态
│   ├── news-detail.html     新闻详情
│   ├── join.html            招商加盟
│   └── contact.html         联系我们
│
├── admin/                  后台页面
│   ├── login.html           登录
│   ├── dashboard.html       后台首页
│   ├── products.html        产品管理
│   ├── news.html            新闻管理
│   ├── banners.html         首页轮播图
│   ├── about.html           关于春晖管理
│   ├── join.html            招商内容管理
│   ├── contact.html         联系方式管理
│   └── users.html           账号管理
│
├── server/                 后端程序
│   ├── app.js               服务入口
│   ├── routes/              接口路由
│   ├── services/            上传、压缩、发布逻辑
│   ├── middleware/          登录验证、权限判断
│   └── database/            数据库连接和表结构
│
└── data/
    └── chunhui.sqlite       数据库文件
```

## 3. 推荐技术方案

适合当前项目的轻量方案：

```text
前台：HTML + CSS + JavaScript
后台：HTML + CSS + JavaScript
后端：Node.js + Express
数据库：SQLite
图片处理：sharp
登录认证：服务端 Session
```

原因：

- SQLite 不需要单独安装数据库服务，部署简单。
- Node.js 适合处理上传、图片压缩、接口。
- Express 结构清晰，方便以后扩展。
- sharp 可以自动压缩、裁剪、生成缩略图。

## 4. 前台页面设计

### 首页 `index.html`

用途：品牌第一印象和关键入口。

包含：

- 首页主视觉
- 产品中心入口
- 热门产品 6 到 8 个
- 春晖优势
- 招商加盟入口
- 新闻精选
- 联系表单

### 产品中心 `products.html`

用途：集中展示产品。

包含：

- 产品分类入口
- 左侧筛选
- 产品列表
- 产品图片、名称、分类、标签
- 点击进入产品详情
- 咨询按钮

### 产品详情 `product-detail.html`

用途：单个产品的详细介绍。

包含：

- 产品大图
- 产品名称
- 分类、系列、标签
- 产品卖点
- 材质/工艺/适用空间
- 相关推荐
- 预约咨询

### 关于春晖 `about.html`

用途：展示企业实力。

包含：

- 公司介绍
- 工厂设备
- 品牌历程
- 荣誉资质
- 团队/展厅图片

### 新闻动态 `news.html`

用途：发布品牌新闻和保养知识。

包含：

- 新闻列表
- 分类筛选
- 新闻详情页

### 招商加盟 `join.html`

用途：招商转化。

包含：

- 加盟优势
- 扶持政策
- 门店案例
- 加盟流程
- 留资表单

### 联系我们 `contact.html`

用途：让客户快速找到联系方式。

包含：

- 电话
- 地址
- 邮箱
- 联系人
- 地图
- 留言表单

## 5. 后台功能设计

后台首页要做得简单，不像技术系统，更像“工作台”。

```text
登录成功
  |
  v
后台首页
  ├── 修改首页内容
  ├── 上传产品
  ├── 发布新闻
  ├── 修改联系方式
  └── 查看最近修改
```

### 5.1 登录

字段：

- 账号
- 密码

功能：

- 登录失败提示
- 登录成功进入后台
- 长时间不操作自动退出
- 修改密码

### 5.2 产品管理

列表功能：

- 搜索产品名称
- 按分类筛选
- 上架 / 下架
- 编辑
- 删除
- 拖动排序

新增产品表单：

```text
产品名称
产品分类
产品系列
产品标签
产品简介
产品卖点
产品图片
是否首页推荐
排序
状态：草稿 / 已发布
```

图片上传体验：

- 支持点击上传
- 支持拖拽上传
- 上传后自动压缩
- 自动生成缩略图
- 自动改文件名
- 可以手动裁剪显示区域

按钮：

```text
保存草稿
预览
发布
```

### 5.3 新闻管理

字段：

```text
新闻标题
新闻分类
发布日期
封面图
摘要
正文内容
状态：草稿 / 已发布
```

编辑器要求：

- 像写公众号文章一样编辑
- 支持插入图片
- 支持加粗、标题、列表
- 支持预览

### 5.4 首页内容管理

可修改：

- 首页主标题
- 副标题
- 首页按钮文字
- 首页轮播图
- 首页推荐产品
- 首页新闻
- 招商引导语

### 5.5 关于春晖管理

可修改：

- 公司介绍
- 工厂图片
- 品牌历程
- 企业优势
- 荣誉图片

### 5.6 招商加盟管理

可修改：

- 招商标题
- 加盟优势
- 扶持政策
- 加盟流程
- 门店案例
- 招商联系电话

### 5.7 联系方式管理

可修改：

- 公司名称
- 电话
- 手机
- 邮箱
- 联系人
- 地址
- 备案号
- 二维码

### 5.8 账号管理

角色：

```text
超级管理员：可以管理所有内容和账号
内容管理员：可以管理产品、新闻、页面内容
普通编辑：只能保存草稿，不能发布
```

## 6. 数据库设计

### 用户表 `users`

```text
id
username
password_hash
name
role
status
created_at
updated_at
```

### 产品表 `products`

```text
id
name
category
series
tag
summary
description
features
cover_image
gallery_images
is_featured
sort_order
status
created_at
updated_at
```

### 新闻表 `news`

```text
id
title
category
summary
content
cover_image
publish_date
status
created_at
updated_at
```

### 页面内容表 `page_sections`

适合保存首页、关于春晖、招商加盟等可编辑内容。

```text
id
page_key
section_key
title
subtitle
content
image
extra_json
updated_at
```

### 上传文件表 `media`

```text
id
file_name
original_name
file_path
mime_type
size
width
height
uploaded_by
created_at
```

### 操作记录表 `operation_logs`

用于误操作追踪。

```text
id
user_id
action
target_type
target_id
detail
created_at
```

## 7. 上传图片流程

员工操作：

```text
点击上传图片
选择图片
系统显示预览
员工拖动裁剪框
点击确定
```

系统自动做：

```text
检查图片格式
压缩图片
生成网页图
生成缩略图
自动命名文件
保存到 uploads
把图片地址写入数据库
```

推荐生成：

```text
原图备份：uploads/original/
网页图：uploads/large/
缩略图：uploads/thumb/
```

## 8. 后台界面原则

为了让不熟悉电脑的人也能用：

- 所有页面都有明显的“保存草稿 / 预览 / 发布”按钮。
- 表单字段写中文，不写技术名词。
- 图片区域直接写“点击上传图片”。
- 上传失败时告诉用户原因，比如“图片太大，系统已帮你压缩失败，请换一张”。
- 删除操作要二次确认。
- 发布前可以预览。
- 最近修改可以恢复上一版。
- 不让员工接触文件路径、代码、数据库。

## 9. 接口设计

### 登录

```text
POST /api/auth/login
POST /api/auth/logout
GET  /api/auth/me
```

### 产品

```text
GET    /api/products
GET    /api/products/:id
POST   /api/products
PUT    /api/products/:id
DELETE /api/products/:id
POST   /api/products/:id/publish
POST   /api/products/:id/unpublish
```

### 新闻

```text
GET    /api/news
GET    /api/news/:id
POST   /api/news
PUT    /api/news/:id
DELETE /api/news/:id
POST   /api/news/:id/publish
```

### 页面内容

```text
GET /api/pages/:pageKey
PUT /api/pages/:pageKey
```

### 图片上传

```text
POST /api/media/upload
GET  /api/media
DELETE /api/media/:id
```

## 10. 发布方式

推荐两种发布方式。

### 方式 A：动态读取数据库

前台页面每次打开时从接口读取数据库内容。

优点：

- 修改后立即生效
- 开发简单

缺点：

- 需要后端一直运行

### 方式 B：后台发布后生成静态页面

后台点击发布后，系统自动生成新的 HTML 文件。

优点：

- 前台访问速度快
- 更适合企业官网
- 服务器压力小

缺点：

- 开发稍复杂

建议：第一版先用方式 A，后续稳定后再升级成方式 B。

## 11. 第一阶段开发范围

建议先做最小可用版本：

```text
1. 多页面前台
2. 后台登录
3. 产品管理
4. 新闻管理
5. 联系方式管理
6. 图片上传和自动压缩
```

暂时不做：

```text
复杂权限
版本回滚
批量导入
静态页面生成
高级数据统计
```

## 12. 开发顺序

```text
第一步：把前台拆成多页面
第二步：建立后端服务和数据库
第三步：实现登录
第四步：实现图片上传
第五步：实现产品管理
第六步：让产品中心读取数据库
第七步：实现新闻管理
第八步：实现联系方式管理
第九步：整体测试和部署
```

## 13. 最终员工使用流程

### 上传一个新产品

```text
登录后台
点击“产品管理”
点击“新增产品”
填写产品名称和简介
选择产品分类
上传产品图片
点击“预览”
确认没问题后点击“发布”
```

### 修改联系电话

```text
登录后台
点击“联系方式”
修改电话
点击“保存并发布”
前台自动更新
```

### 发布一条新闻

```text
登录后台
点击“新闻管理”
点击“新增新闻”
填写标题、封面、正文
点击“预览”
点击“发布”
```

